nav#nav(role='navigation')

    #nav-button
        i.icon.icon-menu menu

    ul

        +navItem(
            'skills',
            'Expertise'
        )

        +navItem(
            'work',
            'Projects'
        )

        +navItem(
            'process',
            'Process'
        )

        +navItem(
            'timeline',
            'Timeline'
        )

        +navItem(
            'contact',
            'Contact'
        )
